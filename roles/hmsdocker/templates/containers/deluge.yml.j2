services:
  deluge:
    image: binhex/arch-delugevpn
    container_name: deluge
    restart: ${RESTART_POLICY}
    logging:
      options:
        max-size: "12m"
        max-file: "5"
      driver: json-file
    networks:
      - download_net
      - proxy_net
    {% if hmsdocker_vpn_type == 'wireguard' %}
    privileged: true
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
    {% else %}
    cap_add:
      - NET_ADMIN
    {% endif %}
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TIMEZONE}
      - VPN_ENABLED=yes
      - VPN_USER=${VPN_USER}
      - VPN_PASS=${VPN_PASS}
      - VPN_PROV=custom
      - VPN_CLIENT={{ hmsdocker_vpn_type }}
      - ENABLE_PRIVOXY=yes
      - LAN_NETWORK={{ hms_docker_network_subnet }}
    volumes:
      - ${HMSD_APPS_PATH}/deluge/config:/config
      - ${HMSD_MOUNT_PATH}:/data
    {% if 'deluge' in expose_ports_enabled_containers %}
    ports:
      - 8112:8112
      - 8119:8118 # privoxy
    {% endif %}
    {% if 'deluge' in traefik_enabled_containers or 'deluge' in homepage_enabled_containers %}
    labels:
      {% if 'deluge' in traefik_enabled_containers %}
      - traefik.enable=true
      - traefik.http.services.deluge-${COMPOSE_PROJECT}.loadbalancer.server.port=8112
      - traefik.http.routers.deluge-${COMPOSE_PROJECT}.rule=Host(`{{ hms_docker_container_map['deluge']['proxy_host_rule'] | default('deluge') }}.${HMSD_DOMAIN}`)
      - traefik.http.routers.deluge-${COMPOSE_PROJECT}.middlewares={{ 'external' if 'deluge' in expose_public_enabled_containers else 'internal' }}-{{ 'secured' if traefik_security_hardening else 'ipallowlist' }}@file{{ ',authentik-proxy-${COMPOSE_PROJECT}-deluge-midware@docker' if 'deluge' in authentik_enabled_containers }}
      {% endif %}
      {% if 'deluge' in homepage_enabled_containers %}
      - homepage.group=Downloads
      - homepage.name=deluge
      - homepage.icon=deluge.png
      - homepage.href=http://{{ hms_docker_container_map['deluge']['proxy_host_rule'] | default('deluge') }}.${HMSD_DOMAIN}
      - homepage.description=Torrent Management
      - homepage.widget.type=deluge
      - homepage.widget.url=http://deluge:8112
        {% if 'deluge' in homepage_stats_enabled_containers %}
      - homepage.showStats=true
        {% endif %}
      {% endif %}
    {% endif %}
